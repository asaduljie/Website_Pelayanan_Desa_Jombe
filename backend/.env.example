PORT=5000
NODE_ENV=development
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/jombe_digital_db?schema=public&sslmode=prefer"

JWT_SECRET="jombe_digital_secure_jwt_secret_key_2026_super_encrypted"
JWT_EXPIRES_IN="7d"

ENCRYPTION_KEY="32_bytes_secret_key_for_aes_256_gcm_jombe"

CORS_ORIGIN="http://localhost:3000"

# Storage Settings (local | s3)
STORAGE_TYPE="local"
UPLOAD_DIR="./uploads"

# WhatsApp Business / Gateway (Fonnte/Wablas/Official)
WHATSAPP_API_URL=""
WHATSAPP_API_KEY=""
OPERATOR_WHATSAPP_NUMBER="6281234567890"
# WA session credentials are encrypted and stored in PostgreSQL/Supabase.
# Keep ENCRYPTION_KEY stable; changing it invalidates the stored WA session.

# AI Assistant Settings
AI_SERVICE_ENABLED=true
